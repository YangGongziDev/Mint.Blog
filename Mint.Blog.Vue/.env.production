# 基础路径,默认使用根路径；如果部署到子目录，必须以 / 结尾 / Default is root path; if deployed to a subdirectory, it must end with /
VITE_BASE_URL=/

# 应用标题 / Application title
VITE_APP_TITLE=杨工子

# 生产环境 API 地址：通过 Nginx 反向代理，写绝对路径 /api/ 即可
VITE_SERVICE_BASE_URL=/api/
