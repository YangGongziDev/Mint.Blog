# Mint 全系统主题规范

## 目的

本文档用于统一 `Mint` 系统的日间和暗色主题规则，适用于当前已有的 `Blog`、`System`，以及未来扩展的 `MES` 等业务域。

本规范只做一件事：

- 让整个系统按照同一套主题结构长期演进，而不是每个模块各自定义颜色。

适用范围：

- `Mint.Blog.Vue/src/views`
- `Mint.Blog.Vue/src/components`
- `Mint.Blog.Vue/src/layouts`
- `Mint.Blog.Vue/src/theme`
- 后续新增的 `MES` 或其他业务域前端模块

参考来源：

- [settings.ts](file:///Users/yangmufa/UserDevelopment/CSharp/Mint.Blog/Mint.Blog.Vue/src/theme/settings.ts)
- [home.vue](file:///Users/yangmufa/UserDevelopment/CSharp/Mint.Blog/Mint.Blog.Vue/src/views/blog-surfer/home.vue)
- [article-detail.vue](file:///Users/yangmufa/UserDevelopment/CSharp/Mint.Blog/Mint.Blog.Vue/src/views/blog-surfer/article-detail.vue)
- [login.vue](file:///Users/yangmufa/UserDevelopment/CSharp/Mint.Blog/Mint.Blog.Vue/src/views/system/login.vue)

---

## 一、主题总决策

全系统主题统一按照以下结论执行：

1. 不再为单个模块单独定义一整套主题，而是建立一套全系统可扩展主题规范。
2. 主题拆分为 `基础主题层` 和 `业务域强调层`。
3. `Blog`、`System`、未来 `MES` 共用同一套基础主题。
4. 不同业务域只补自己的强调色，不重定义整套底色和语义色。
5. 暗色模式下，整个系统尽量统一为蓝灰基调，避免每个模块各用一套高饱和主色。

这是本文档的最高原则。后续所有页面、组件、主题 token、样式变量，都必须服从这五条。

---

## 二、主题结构

### 1. 基础主题层

基础主题层由全系统共享，负责：

- 页面背景
- 容器背景
- 浮层背景
- 标题文字
- 正文文字
- 弱化文字
- 常规边框
- 阴影
- 通用语义色

这一层不区分 `Blog`、`System`、`MES`。

### 2. 业务域强调层

业务域强调层只负责模块辨识度，主要影响：

- 主按钮
- 激活态
- 标签强调色
- 页面局部装饰
- 模块标题强调色
- 图表主色

这一层才区分 `Blog`、`System`、`MES`。

### 3. 依赖顺序

实际使用时，按下面顺序取值：

1. 先取基础主题层
2. 再取全系统语义色
3. 最后按业务域补强调色

禁止反过来做，即：

- 不要先定业务域色，再倒推页面背景和正文色
- 不要让某个业务域重写整套基础主题

---

## 三、基础主题层

### 日间基础主题

| Token | 颜色值 | 用途 |
| --- | --- | --- |
| `bg-page` | `#F7FAFC` | 全局页面背景 |
| `bg-container` | `#FFFFFF` | 主容器、卡片、表单主体 |
| `bg-elevated` | `#FFFFFF` | 弹层、下拉、浮层 |
| `bg-soft` | `#F5F7FA` | 浅提示区、表头、分组区 |
| `text-title` | `#1F2937` | 一级标题、关键标题 |
| `text-body` | `#475569` | 正文、说明、表单文本 |
| `text-secondary` | `#64748B` | 二级说明 |
| `text-muted` | `#94A3B8` | 占位、弱提示 |
| `border-default` | `#E2E8F0` | 常规边框 |
| `border-soft` | `#EDF2F7` | 更轻边框、分割线 |
| `shadow-card` | `rgba(15, 23, 42, 0.06)` | 卡片阴影 |
| `shadow-popup` | `rgba(15, 23, 42, 0.12)` | 弹层阴影 |

### 暗色基础主题

| Token | 颜色值 | 用途 |
| --- | --- | --- |
| `bg-page` | `#232931` | 全局页面背景 |
| `bg-container` | `#2C333E` | 主容器、卡片、面板 |
| `bg-elevated` | `#313A46` | 浮层、弹窗、抽屉 |
| `bg-soft` | `#374151` | 弱区块、二级区域 |
| `bg-deep` | `#111827` | Hero、代码块、深色底部 |
| `text-title` | `#F8FAFC` | 一级标题 |
| `text-body` | `#CBD5E1` | 正文、说明 |
| `text-secondary` | `#E2E8F0` | 二级标题、强调说明 |
| `text-muted` | `#94A3B8` | 弱提示、占位 |
| `border-default` | `#334155` | 常规边框 |
| `border-soft` | `rgba(148, 163, 184, 0.16)` | 柔和边框 |
| `shadow-card` | `rgba(0, 0, 0, 0.28)` | 卡片阴影 |
| `shadow-popup` | `rgba(0, 0, 0, 0.36)` | 浮层阴影 |

### 基础主题层规则

- 所有业务域共用这套底色、文字、边框和阴影
- 不允许 `Blog`、`System`、`MES` 单独改 `bg-page`、`text-body`、`border-default`
- 暗色模式统一使用深灰蓝体系，不允许每个业务域发明一套不同暗色底盘

---

## 四、全系统语义色

### 日间语义色

| 语义 | 颜色值 | 用途 |
| --- | --- | --- |
| `info` | `#539DFD` | 信息提示、说明、辅助链接 |
| `success` | `#3ECF9A` | 成功、完成、通过 |
| `warning` | `#F5A630` | 警告、注意、待处理 |
| `error` | `#EB373A` | 错误、删除、失败 |
| `accent` | `#A543E6` | 次强调、装饰、高亮 |

### 暗色语义色

| 语义 | 颜色值 | 用途 |
| --- | --- | --- |
| `info` | `#60A5FA` | 信息提示 |
| `success` | `#38BDF8` | 成功反馈 |
| `warning` | `#F59E0B` | 警告、提醒 |
| `error` | `#F87171` | 错误、危险操作 |
| `accent` | `#A78BFA` | 次强调、装饰 |

### 语义色规则

- 语义色属于全系统，不属于某个业务域
- `success`、`warning`、`error` 不允许按业务域各自定义
- 暗色模式中，`success` 优先使用青蓝，不继续使用高饱和绿色

---

## 五、业务域强调层

### Blog

适用范围：

- `blog-surfer`
- `blog-admin` 中偏内容展示和内容编辑的页面

日间强调色：

- `brand-primary`: `#3ECF9A`
- `brand-hover`: `#2BC48D`
- `brand-active`: `#15956B`
- `brand-soft`: `rgba(62, 207, 154, 0.10)`
- `brand-border`: `rgba(62, 207, 154, 0.18)`

暗色强调色：

- `brand-primary`: `#539DFD`
- `brand-hover`: `#60A5FA`
- `brand-active`: `#3B82F6`
- `brand-soft`: `rgba(83, 157, 253, 0.10)`
- `brand-border`: `rgba(83, 157, 253, 0.18)`

规则：

- `Blog` 日间保留薄荷绿
- `Blog` 暗色统一切换到蓝色
- `Blog` 暗色下不再把绿色作为主视觉色

### System

适用范围：

- 登录页
- 用户、角色、菜单、权限、系统配置
- 后台通用壳层

日间强调色：

- `brand-primary`: `#4F7CFF`
- `brand-hover`: `#3E6EF7`
- `brand-active`: `#3158D6`
- `brand-soft`: `rgba(79, 124, 255, 0.10)`
- `brand-border`: `rgba(79, 124, 255, 0.18)`

暗色强调色：

- `brand-primary`: `#539DFD`
- `brand-hover`: `#60A5FA`
- `brand-active`: `#3B82F6`
- `brand-soft`: `rgba(83, 157, 253, 0.10)`
- `brand-border`: `rgba(83, 157, 253, 0.18)`

规则：

- `System` 优先表达稳定、专业、管理感
- 装饰渐变可以存在，但交互主色仍以蓝色为核心

### MES

适用范围：

- 工单
- 设备
- 产线
- 报工
- 生产单
- 质检

日间强调色：

- `brand-primary`: `#F59E0B`
- `brand-hover`: `#D97706`
- `brand-active`: `#B45309`
- `brand-soft`: `rgba(245, 158, 11, 0.10)`
- `brand-border`: `rgba(245, 158, 11, 0.18)`

暗色强调色：

- `brand-primary`: `#60A5FA`
- `brand-hover`: `#3B82F6`
- `brand-active`: `#2563EB`
- `brand-soft`: `rgba(96, 165, 250, 0.10)`
- `brand-border`: `rgba(96, 165, 250, 0.18)`

规则：

- `MES` 日间可以更偏工业感和效率感
- `MES` 暗色仍然回归蓝色体系
- 不允许为 `MES` 单独设计一套高饱和黑橙暗色主题

---

## 六、暗色模式统一规则

暗色模式是全系统统一规则，不是每个业务域各自定义。

### 必须遵守

- 背景统一使用蓝灰底盘
- 主文字统一使用浅灰白层级
- 常规边框统一使用低对比蓝灰边框
- 主强调色优先落在蓝色范围

### 尽量避免

- 大面积使用绿色作为主视觉
- 同一页面混用高饱和蓝、绿、红三种主强调色
- 延续日间模式的高亮描边和高明度装饰
- 用纯白大面积铺底

### 允许保留

- `warning` 的橙色
- `error` 的红色
- `accent` 的紫色
- 局部高亮用白色

### Blog 前台暗色细则

适用于 `blog-surfer/home`、`blog-surfer/article/:id` 等阅读场景：

- 卡片背景：`#2C333E` 或 `rgba(44, 51, 62, 0.88)`
- 卡片边框：`#334155`
- 标签背景：`rgba(83, 157, 253, 0.10)`
- 标签文字：`#539DFD`
- 链接色：`#539DFD`
- 阅读区标题：`#F8FAFC` / `#E2E8F0`
- 行内代码背景：`rgba(83, 157, 253, 0.10)`
- 引用块边线：`#539DFD`
- 代码块背景：`linear-gradient(145deg, #111827, #1E293B)`

Blog 前台从日间切暗色时，优先做如下映射：

| 日间颜色 | 暗色替换 |
| --- | --- |
| `#3ECF9A` | `#539DFD` |
| `#15956B` | `#3B82F6` |
| `rgba(62, 207, 154, 0.10)` | `rgba(83, 157, 253, 0.10)` |
| `rgba(62, 207, 154, 0.18)` | `rgba(83, 157, 253, 0.18)` |
| `#0D3D2D` | `#F8FAFC` |
| `#49695D` | `#CBD5E1` |
| `#60786E` | `#94A3B8` |

---

## 七、组件使用规则

### 全系统公共组件

例如：

- 按钮
- 输入框
- 表格
- 抽屉
- 弹窗
- Tabs
- 面包屑
- 页头
- 空状态

统一规则：

- 优先使用基础主题层
- 状态反馈统一使用全系统语义色
- 不在组件内部直接绑定 `Blog`、`System`、`MES` 的品牌色

### 业务页面中的强调元素

例如：

- 页面主按钮
- 活跃菜单
- 标签选中态
- 页面 Hero
- 模块标题强调块
- 页面装饰渐变

统一规则：

- 这些元素可以使用业务域强调色
- 但只能替换强调部分，不能反向改基础主题层

---

## 八、后续新增业务域怎么做

如果后续新增 `MES`、`CRM`、`OA`、`WMS` 等业务域，统一按下面步骤扩展：

1. 先复用全系统基础主题层
2. 复用全系统语义色
3. 新增该业务域自己的强调色
4. 暗色模式仍然回归蓝灰底盘

不要这样做：

- 新业务域重新定义整套背景、文字、边框、状态色
- 为新业务域单独做另一套暗色底色系统
- 每个页面都自己写一套颜色规则

---

## 九、推荐 Token 结构

推荐分三层建 token：

### 1. 基础 token

```ts
type FoundationTokens = {
  colorBgPage: string;
  colorBgContainer: string;
  colorBgElevated: string;
  colorBgSoft: string;
  colorTextTitle: string;
  colorTextBody: string;
  colorTextSecondary: string;
  colorTextMuted: string;
  colorBorderDefault: string;
  colorBorderSoft: string;
  shadowCard: string;
  shadowPopup: string;
};
```

### 2. 语义 token

```ts
type SemanticTokens = {
  colorInfo: string;
  colorSuccess: string;
  colorWarning: string;
  colorError: string;
  colorAccent: string;
};
```

### 3. 业务域 token

```ts
type DomainBrandTokens = {
  colorBrandPrimary: string;
  colorBrandHover: string;
  colorBrandActive: string;
  colorBrandSoft: string;
  colorBrandBorder: string;
};
```

### 4. 推荐结构

```ts
export const mintThemeTokens = {
  light: {
    foundation: {
      colorBgPage: '#F7FAFC',
      colorBgContainer: '#FFFFFF',
      colorBgElevated: '#FFFFFF',
      colorBgSoft: '#F5F7FA',
      colorTextTitle: '#1F2937',
      colorTextBody: '#475569',
      colorTextSecondary: '#64748B',
      colorTextMuted: '#94A3B8',
      colorBorderDefault: '#E2E8F0',
      colorBorderSoft: '#EDF2F7',
      shadowCard: '0 4px 24px rgba(15, 23, 42, 0.06)',
      shadowPopup: '0 12px 32px rgba(15, 23, 42, 0.12)'
    },
    semantic: {
      colorInfo: '#539DFD',
      colorSuccess: '#3ECF9A',
      colorWarning: '#F5A630',
      colorError: '#EB373A',
      colorAccent: '#A543E6'
    },
    domain: {
      blog: {
        colorBrandPrimary: '#3ECF9A',
        colorBrandHover: '#2BC48D',
        colorBrandActive: '#15956B',
        colorBrandSoft: 'rgba(62, 207, 154, 0.10)',
        colorBrandBorder: 'rgba(62, 207, 154, 0.18)'
      },
      system: {
        colorBrandPrimary: '#4F7CFF',
        colorBrandHover: '#3E6EF7',
        colorBrandActive: '#3158D6',
        colorBrandSoft: 'rgba(79, 124, 255, 0.10)',
        colorBrandBorder: 'rgba(79, 124, 255, 0.18)'
      },
      mes: {
        colorBrandPrimary: '#F59E0B',
        colorBrandHover: '#D97706',
        colorBrandActive: '#B45309',
        colorBrandSoft: 'rgba(245, 158, 11, 0.10)',
        colorBrandBorder: 'rgba(245, 158, 11, 0.18)'
      }
    }
  },
  dark: {
    foundation: {
      colorBgPage: '#232931',
      colorBgContainer: '#2C333E',
      colorBgElevated: '#313A46',
      colorBgSoft: '#374151',
      colorTextTitle: '#F8FAFC',
      colorTextBody: '#CBD5E1',
      colorTextSecondary: '#E2E8F0',
      colorTextMuted: '#94A3B8',
      colorBorderDefault: '#334155',
      colorBorderSoft: 'rgba(148, 163, 184, 0.16)',
      shadowCard: '0 18px 52px rgba(0, 0, 0, 0.28)',
      shadowPopup: '0 24px 60px rgba(0, 0, 0, 0.36)'
    },
    semantic: {
      colorInfo: '#60A5FA',
      colorSuccess: '#38BDF8',
      colorWarning: '#F59E0B',
      colorError: '#F87171',
      colorAccent: '#A78BFA'
    },
    domain: {
      blog: {
        colorBrandPrimary: '#539DFD',
        colorBrandHover: '#60A5FA',
        colorBrandActive: '#3B82F6',
        colorBrandSoft: 'rgba(83, 157, 253, 0.10)',
        colorBrandBorder: 'rgba(83, 157, 253, 0.18)'
      },
      system: {
        colorBrandPrimary: '#539DFD',
        colorBrandHover: '#60A5FA',
        colorBrandActive: '#3B82F6',
        colorBrandSoft: 'rgba(83, 157, 253, 0.10)',
        colorBrandBorder: 'rgba(83, 157, 253, 0.18)'
      },
      mes: {
        colorBrandPrimary: '#60A5FA',
        colorBrandHover: '#3B82F6',
        colorBrandActive: '#2563EB',
        colorBrandSoft: 'rgba(96, 165, 250, 0.10)',
        colorBrandBorder: 'rgba(96, 165, 250, 0.18)'
      }
    }
  }
} as const;
```

---

## 十、落地检查清单

每次新增页面、组件或主题变量前，先检查：

1. 这次要改的是基础主题层，还是业务域强调层。
2. 如果是背景、文字、边框、阴影，是否错误地写到了业务域里。
3. 如果是主按钮、激活态、标签强调色，是否应该用业务域强调色。
4. 暗色模式是否回到了蓝灰底盘。
5. 是否错误地为某个模块重新定义了整套语义色。
6. 是否在页面里散落大量硬编码十六进制颜色。

---

## 十一、最终结论

- 你现在需要的不是一个 `blog-surfer` 专用色板，而是一套全系统可扩展主题规范
- 最合适的做法是把主题拆成 `基础主题层 + 业务域强调层`
- `Blog`、`System`、未来 `MES` 都共用同一套基础主题
- 不同业务域只补自己的强调色，不重定义整套底色和语义色
- 暗色模式下，整个系统尽量统一为蓝灰基调，避免每个模块各用一套高饱和主色
